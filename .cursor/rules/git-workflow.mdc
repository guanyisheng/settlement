---
description: Automatic Git workflow — branch, commit as guanyisheng, push, PR (never merge main)
alwaysApply: true
---

# Git 自动开发规范

完成功能或 Bug 后**自动**执行完整流程，无需询问确认：

Issue → Branch → 开发 → Commit → Push → Pull Request

## 分支

- 新功能：`feature/xxx`（从最新 `main` 检出）
- Bug：`fix/xxx`
- **禁止**在 `main` 上直接开发或 push `main`

## Commit

格式：

- `feat: …`
- `fix: …`
- `refactor: …`
- `docs: …`

Author / Committer **必须**为：

- Name: `guanyisheng`
- Email: `208835232+guanyisheng@users.noreply.github.com`

禁止 Cursor / AI / Agent / OpenAI 等身份。若工具自动插入 `Co-authored-by: Cursor`，提交后、**push 前**必须从 commit message 中剔除（本地 rewrite 未推送提交即可；禁止 force push 已共享历史）。

## Push / PR

- 测试/自检通过后：`git push -u origin <当前分支>`
- 自动创建 PR：`当前分支 → main`（用 `gh pr create`）
- **禁止**自动 Merge 到 `main`

## 禁止

- push `main`
- force push
- `reset --hard`
- 删除远程分支
- 修改 `main` 历史
- 提交 `.env`、密码、Token、SecretKey、本地密钥配置

## 角色

- Cursor = 开发工具
- guanyisheng = Git Author
- feature/fix = 开发环境
- main = 稳定版本
