# 清账系统 · 业务功能说明

面向老板 / 客服 / 打手的完整功能清单与结算规则说明。  
技术部署见 [`README.md`](README.md)，日常操作见 [`使用手册.md`](使用手册.md)。

---

## 1. 系统是做什么的

内部清账：**报单 → 审核结算 → 余额 → 提现申请 → 人工扫码放款**。

- 不接微信/支付宝支付接口，不做自动打款
- 三端：公开登录 / 打手移动端 `/staff/` / 管理后台 `/admin/`
- 技术栈：PHP 8 + MySQL，可选腾讯云 COS

---

## 2. 角色与入口

| 角色 | 主要入口 | 典型能力 |
|------|----------|----------|
| 打手 | 手机打手端 | 报单、看订单/余额、提现、个人资料 |
| 客服 / 员工 | PC 后台 | 审单、放款、客户、业务类型等（按权限） |
| 老板 | PC 后台（可进打手端） | 全部菜单；可改费率、角色权限 |
| 考官等自定义角色 | 按 RBAC 勾选权限 | 例如只看荣誉、只审注册 |

- 一人可多角色，权限取并集
- 纯打手不能进 PC 后台；有后台权限且也能报单的账号，登录后可选进后台或打手端
- 支持「记住登录」约 30 天

---

## 3. 打手端功能

### 3.1 注册与登录
- 公开注册（用户名实时查重），默认待审核
- 后台「注册审核」通过后才能正常使用
- 登录后可改自己的昵称、毛照、荣誉、收款码、密码（个人中心）

### 3.2 我的订单
- 顶部：可提现余额、累计收入等
- 列表：微信订单号、客户、业务、数量、金额、接单时间、状态
- 状态：待审核 / 已通过 / 已拒绝 / 已结算
- **仅「已通过 / 已结算」计入可提现余额**
- 双人单会标注主报单 / 附加打手，金额显示**本人份额**

### 3.3 我要报单
- 搜索选择客户、业务类型（后台维护，非写死）
- **接单方式**：一人接单（默认）/ 双人接单
  - 双人须搜索选择附加打手
- 微信订单编号必填、**全局防重**（同号不能再报）
- 订单截图最多 9 张
- 数量、接单开始/结束时间、备注
- 实时预览：订单金额、按一人/双人费率的预计到手

### 3.4 提现
- 按可提现余额申请
- **每天最多申请一次**（业务规则）
- 记录列表；放款依赖后台人工确认
- 提现前建议在个人中心上传收款二维码

### 3.5 个人中心 / 毛照 / 荣誉
- 昵称、多图毛照、荣誉图文、收款码、改密
- 老板/客服等能进打手端的账号也可改自己的资料

---

## 4. 管理后台功能

### 4.1 工作台
- 今日报单、待审核、待提现等概览

### 4.2 订单管理
- 筛选（状态、打手、客户、日期等）
- 审核通过 / 拒绝（拒绝需原因）
- 预存客户：通过时可扣客户余额（余额不足则无法通过）
- 特殊单：可改本单基础倍率、打手倍率，或勾选手动金额
- 展示主打手 + 附加打手
- 支持删除（按权限）

### 4.3 提现管理
- 查看申请；详情可看打手收款码
- 确认已放款 / 拒绝；事务防重复扣款

### 4.4 用户中心（统一）
- 合并原「打手 / 员工」列表与详情
- 搜索、角色筛选；添加打手 / 员工
- 详情：档案、状态、角色、重置密码
- 打手向：入职/考核/押金、毛照、荣誉、收入与订单/提现流水
- 旧链接 `staff.php` / `employees.php` / `staff_detail.php` 会跳转过来

### 4.5 注册审核
- 处理打手自助注册的待审核账号

### 4.6 客户管理
- 维护客户；可设预存与余额
- 预存单审核通过自动扣款

### 4.7 业务类型 + 默认结算倍率
- 维护业务名称、单价（报单金额 = 单价 × 数量）
- **页顶配置全局默认费率**（见第 5 节）
- 支持关键字搜索业务类型

### 4.8 数据统计
- 总览：打手数、客户、预存余额、累计订单/结算/放款等
- 订单结算区：快捷区间（今天/昨天/本周/上周/本月/上月/总共）
- **自定义起止日期**查询
- 区间内：已通过订单数（按审核日）、流水、提现、抽成（流水 − 打手结算）
- 报单趋势等图表

### 4.9 角色与权限（RBAC）
- 角色列表、编辑权限点
- 菜单按权限显示
- 系统角色不可乱删

### 4.10 系统设置
- 品牌名、Logo、主题色、版本号
- 存储：本地 / 腾讯云 COS（密钥无效可降级本地）

### 4.11 其他
- 修改自己的登录密码
- 管理端移动适配：侧栏汉堡、竖屏提示
- 关键提示可带错误码（如 `【E-RPT-0001】`）便于排查

---

## 5. 结算与费率（可后台改）

### 5.1 在哪里改

**管理后台 → 业务类型 → 页顶部「默认结算倍率」**

也可书签旧地址 `/admin/rates.php`（会跳转到同一位置）。

可分别设置：

| 配置项 | 含义 | 默认示例 |
|--------|------|----------|
| 基础倍率 | 所有单共用的第一层系数 | 80% |
| **一人接单 · 打手倍率** | 一个人接时的第二层系数 | 100% |
| **双人接单 · 每人倍率** | 两个人时每个人的第二层系数 | 50% |

保存后**只影响新报单**；已落库订单用当时的倍率快照，不重算。

### 5.2 计算公式

```
订单金额 = 业务单价 × 数量

一人到手 = 订单金额 × 基础倍率 × 一人打手倍率
双人每人 = 订单金额 × 基础倍率 × 双人每人倍率
双人合计（落库 staff_amount）= 双人每人 × 2
```

示例（金额 ¥100，基础 80%，一人 100%，双人每人 50%）：

| 方式 | 结果 |
|------|------|
| 一人 | 打手拿到 **¥80**（80×100） |
| 双人 | 每人 **¥40**（80×50），合计 ¥80 |

可按团队规则改成任意合法百分比，例如一人 90%、双人每人 45%，不必强制「一人 = 双人×2」。

### 5.3 本单覆盖
- 订单详情审核时：可改本单倍率，或勾选手动填写结算金额
- 改倍率会按一人/双人规则重算（双人单的「打手倍率」按每人倍率理解）

### 5.4 余额与抽成口径
- 打手累计收入 = 已通过/已结算订单中**本人份额**之和（双人已平分）
- 可提现 = 累计收入 − 已放款 − 待处理提现
- 统计「抽成」≈ 区间内订单流水 − 打手结算总额（`amount − staff_amount`）

### 5.5 业务单价 vs 费率
- **业务类型单价**：客户侧/订单标价（「一个月套餐多少钱」这类，用业务名称 + 单价配置）
- **结算费率**：从订单金额里算出打手拿多少（一人多少、两人每人多少）
- 两者分开：先定业务卖多少钱，再定打手按什么比例结算

---

## 6. 双人单协作规则

- 同一微信订单号全库唯一；主报单人选「双人」并指定搭档
- 附加打手订单列表可见该单；审核通过后两边余额各记一半份额
- 未选搭档不能提交双人单
- 报单搜索附加打手：只搜启用中的打手，不含纯管理员/客服（报单场景预期行为）

---

## 7. 核心业务流程

```
打手注册 → 客服/老板审核启用
    ↓
打手报单（一人/双人）→ 待审核
    ↓
客服审核通过（预存客户扣余额）→ 打手余额增加
    ↓
打手申请提现（一天一次）→ 客服看收款码线下打款 → 确认已放款
```

拒绝订单 / 拒绝提现：不计入或释放相应额度。

---

## 8. 数据与安全要点

- 金额、倍率在服务端计算；报单写入 `rate_a` / `rate_b` / `staff_amount` 快照
- 微信订单号防重；截图与毛照有大小与数量限制
- 密码哈希存储；后台按权限点控制菜单与操作
- 软删除用户/部分资源，避免误删丢历史

---

## 9. 页面索引（常用）

| 端 | 路径 | 说明 |
|----|------|------|
| 公开 | `/login.php` `/register.php` | 登录 / 注册 |
| 打手 | `/staff/index.php` | 订单 |
| 打手 | `/staff/report.php` | 报单 |
| 打手 | `/staff/withdrawals.php` | 提现 |
| 打手 | `/staff/profile.php` | 个人中心 |
| 后台 | `/admin/index.php` | 工作台 |
| 后台 | `/admin/orders.php` | 订单 |
| 后台 | `/admin/withdrawals.php` | 提现 |
| 后台 | `/admin/users.php` | 用户中心 |
| 后台 | `/admin/business_types.php#settlement` | 业务类型 + **改费率** |
| 后台 | `/admin/statistics.php` | 数据统计 |
| 后台 | `/admin/customers.php` | 客户 |
| 后台 | `/admin/roles.php` | 角色权限 |
| 后台 | `/admin/settings.php` | 系统设置 |

---

## 10. 版本与文档关系

| 文档 | 用途 |
|------|------|
| 本文件 `业务功能说明.md` | 业务功能与结算规则总览 |
| `使用手册.md` | 给使用者看的操作步骤 |
| `README.md` | 安装部署与技术说明 |

费率或一人/双人规则若再调整，以 **业务类型页当前配置** 与本文件第 5 节为准。
